configurations {
    docker {
        transitive = false
    }
}

dependencies {
    docker project(':alfresco-extension')
}

task copyDependencies(type: Copy) {
    from configurations.docker
    into "$buildDir/docker/"
}

build.dependsOn copyDependencies
